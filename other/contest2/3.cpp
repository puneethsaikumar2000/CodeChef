#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

typedef pair<int, int> pi;

int main()
{
	int n;
	cin>>n;
	vector<pi> v(n);
	
	for (int i = 0; i < n; ++i)
	{
		int a;
		cin >> a;
		v[i] = a;
		
	}
	
	return 0;
}